<UserControl x:Class="Console.WPF.Views.DiagramView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="8">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="扫描仪器" Padding="12,4" Click="Scan"/>
            <TextBlock Margin="12,0" VerticalAlignment="Center" Foreground="Gray"
                       Text="点击绿色/灰色仪器节点, 在扫描列表中为它选择 VISA 地址"/>
        </StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/><ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ScrollViewer Grid.Column="0"><Canvas x:Name="Canvas" Height="420" Background="#0B1220"/></ScrollViewer>
            <DockPanel Grid.Column="1" Margin="8,0,0,0">
                <TextBlock DockPanel.Dock="Top" Text="已扫描仪器" FontWeight="Bold" Margin="0,0,0,6"/>
                <ListBox x:Name="ScanList">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="4">
                                <TextBlock Text="{Binding Visa}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Idn}" Foreground="Gray" FontSize="11" TextWrapping="Wrap"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Grid>
    </DockPanel>
</UserControl>
