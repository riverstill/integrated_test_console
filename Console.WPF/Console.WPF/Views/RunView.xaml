<UserControl x:Class="Console.WPF.Views.RunView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sp="clr-namespace:ScottPlot;assembly=ScottPlot.WPF">
    <DockPanel Margin="8">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="开始运行" Style="{StaticResource PrimaryButton}" Click="Start"/>
            <Button Content="暂停" Margin="8,0" Click="Pause"/>
            <Button Content="继续" Click="Resume"/>
            <Button Content="确认下一步" Margin="8,0" Click="Confirm"/>
            <Button Content="终止" Style="{StaticResource DangerButton}" Click="Stop"/>
            <TextBlock x:Name="Status" Margin="16,0" VerticalAlignment="Center" Text="就绪"/>
        </StackPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="轮标签(逗号分隔):" VerticalAlignment="Center"/>
            <TextBox x:Name="LabelsBox" Width="300" Margin="8,0" Text="DUT_1,DUT_2"/>
            <TextBlock Text="输出目录:" VerticalAlignment="Center" Margin="8,0"/>
            <TextBox x:Name="OutBox" Width="300" Text="results/session1"/>
        </StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <DockPanel Grid.Column="0" Margin="0,0,4,0">
                <TextBlock DockPanel.Dock="Top" Text="实时数据" FontWeight="Bold"/>
                <DataGrid x:Name="Grid" AutoGenerateColumns="True" IsReadOnly="True"/>
            </DockPanel>
            <DockPanel Grid.Column="1" Margin="4,0,0,0">
                <TextBlock DockPanel.Dock="Top" Text="实时曲线 (dB vs Hz, 对数横轴)" FontWeight="Bold"/>
                <sp:WpfPlot x:Name="Plot" />
            </DockPanel>
        </Grid>
        <TextBox x:Name="LogBox" DockPanel.Dock="Bottom" Height="120" Margin="0,8,0,0"
                 IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"/>
    </DockPanel>
</UserControl>
